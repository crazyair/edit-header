import { useState } from 'react';

import { useStorage } from '@plasmohq/storage/hook';

const Options = () => {
    return <div style={{ padding: '20px', maxWidth: '600px' }}></div>;
};

export default Options;
